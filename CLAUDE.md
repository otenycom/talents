# Oteny Talents

The agent guide for this repo lives in **[AGENTS.md](AGENTS.md)** — read it. (Two filenames so
both Claude Code and other agents find their own entry point; `AGENTS.md` is the canonical one,
a real file rather than a symlink so it survives a Windows checkout.)
