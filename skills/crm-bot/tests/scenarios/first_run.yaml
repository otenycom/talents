bot: crm-bot
live_only: true

turns:
  - user: "set up CRM for the booth"
    expect:
      reply:
        contains_any: ["Odoo", "CRM", "Shall I", "install"]
        not_contains:
          - "lead-bot.oteny.bot"
          - "nightly.odoo.com"
          - "CRM is ready"
