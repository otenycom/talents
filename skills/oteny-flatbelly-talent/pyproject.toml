[project]
name = "oteny-flatbelly-talent"
version = "0"
description = "Runtime deps for OtenyFlatBellyTalent tenant scripts (dashboard cron)."
requires-python = ">=3.12,<3.13"
dependencies = [
    "matplotlib==3.11.0",
]

[tool.uv]
package = false
