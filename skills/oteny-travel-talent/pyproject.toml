[project]
name = "oteny-travel-talent"
version = "0"
description = "Runtime deps for OtenyTravelTalent tenant scripts (trip-card cron)."
requires-python = ">=3.12,<3.13"
dependencies = [
    "matplotlib==3.11.0",
]

[tool.uv]
package = false
