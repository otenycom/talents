<!-- ~/.hermes/memories/USER.md — SHARED cross-skill identity, rendered from profile.yaml
     by first-run. Hermes auto-loads this into EVERY session, so keep it SMALL and
     bot-agnostic: who the user is, not what any one bot tracks. Per-bot DOMAIN memory
     (home city, currency, travel prefs) lives in ~/.hermes/data/<bot>/memory.md.
     Replace every {{placeholder}} from profile.yaml; drop any line whose source is unset. -->
{{name}} — timezone {{home_timezone}}. Reply language: {{language}}.
Prefers compact, scannable, Telegram-friendly replies. Values transparency on errors
(surface them, don't silently work around) and grounded facts over guesses.
